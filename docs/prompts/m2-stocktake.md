M2 の中間棚卸しを行います。実装は行わず、調査と報告のみ。
作業前に最新の main を pull すること。

■ 1. FR-001〜FR-024 の再判定
M1 完了時点（v0.1.0）の対応表を基準に、現在の状態を再判定してください。
- 各要件の「実装済 / 部分実装 / 未実装」と、根拠となるクラス・ファイル
- M1 完了時点から状態が変わったものを明示する（変化の差分が分かる形で）
- 判定はコードを確認して行うこと。M1 の表をそのまま流用しないこと

■ 2. NFR-001〜NFR-010 の再判定
同様に「検証済 / 未検証」を再判定してください。
特に以下は M2 で状況が変わっているはずです。
- NFR-002（キャッシュヒット時 p99 ≤ 20ms）: M2-3 でキャッシュを実装したが
  ステージ単体の計測ができていない（Issue で追跡中）
- NFR-001（可用性・縮退継続）: キャッシュ実装により状況が変わったか

■ 3. Open Issue の棚卸し
- Milestone 別の一覧（M2 / M2完了後 / 未設定）
- M2 の Issue のうち、既に実質的に解決しているのに
  クローズし忘れているものが無いか確認する
  （P11 の棚卸しで6件のクローズ漏れが見つかった前例がある）
- Milestone 未設定の Issue があれば分類すること

■ 4. M2 の完了条件の提案
M1 は P0〜P11 という明確なフェーズ定義がありましたが、
M2 は Issue を個別に消化する形で進んでおり、完了条件が定義されていません。
現在の Open Issue と FR / NFR の状態を踏まえて、
「M2 をどこで完了とするか」の案を提示してください。
- M2 に含めるべき項目と、M3 以降へ送るべき項目の線引き
- その線引きの根拠（何が揃えば一区切りと言えるか）
私が判断するので、案として複数出して構いません。

報告は表形式で、事実と推測を区別して記載すること。

---

（背景）4 を入れているのは、M2 に終わりが定義されていないからです。M1 は
「P0〜P11 を終えてタグを打つ」という明確な終点がありましたが、M2 は Issue を
1件ずつ潰していく形で進んでいて、このままだと残り12件を全部やるまで終わらないのか、
どこかで区切るのかが決まっていません。棚卸しの結果を見て、M2 の終点を決めましょう。
