P11 プロンプト（M1 最終フェーズ）

docs/prompts/p11.md に保存してから着手してください。

M1 の最終フェーズとして、仕上げを行います。
作業前に最新の main から feat/p11-finalize ブランチを切ること。
設計書は §1.8（機能要件 FR-001〜FR-024）、§1.9（非機能要件）、
実装ガイド §6.13 を読む。

■ 実装前に決めること（方針を提示して承認を得てから着手）

1. NFR-002 の測定可否
   設計書 NFR-002 は「Prompt取得（キャッシュヒット時）p99 ≤ 20ms」ですが、
   CompiledPrompt のキャッシュは Issue #15（Template/Fragment の Domain Event 未実装）
   のため M1 では実装していません。
   つまり NFR-002 は現状では測定できません。
   - キャッシュを M1 に含めるか（#15 の解消が前提）
   - NFR-002 を「M1 では未検証」として記録し、回収先を明示するか
   判断と根拠を示してください。

2. 性能測定の方法
   NFR-003（Render p99 ≤ 200ms）をどう測るか。
   - 負荷ツール、シナリオ、データ量（Prompt 数・Fragment 数・依存の深さ）
   - 測定環境（ローカル / CI / コンテナ）と、その環境差をどう扱うか
   - 再現可能な手順として残す方法

■ 実装スコープ
- deploy/docker/Dockerfile（マルチステージ、非 root ユーザ、
  distroless または jre 系の軽量イメージ）
- deploy/helm/prompt-engine/（api / worker / admin の3 Deployment、
  HPA、ConfigMap、Secret 参照、liveness / readiness は actuator）
- tests/prompt-regression: Golden Prompt 回帰テスト
  - サンプル .prompt 一式について renderHash を golden ファイルに固定
  - 差分が出たら失敗し、意図的変更の場合の golden 更新手順を README に書く
  - golden ファイルが0件でも成功する構成にしないこと（0件ガードを入れる）
- README.md の完成
  - アーキテクチャ概要、起動手順、モジュール説明、設計書へのリンク
  - 初期 GitHub 設定手順
  - ローカル開発の手順（compose、pre-commit フック、Docker 必要性）
- 性能測定の実施と結果の記録（上記1・2の方針に従う）

■ M1 完了判定
- 設計書§1.8 の FR-001〜FR-024 それぞれについて
  「実装済 / 部分実装 / 未実装」と、該当クラス・ファイルを表にすること
  推測で「実装済」と書かないこと。根拠となるコードを確認して判定すること
- 部分実装・未実装のものは、対応する Issue と Milestone を紐づける
- 設計書§1.9 の NFR-001〜NFR-010 についても同様に、
  「検証済 / 未検証」と、検証方法または未検証の理由を記載すること
- Open Issue の一覧を Milestone 別に出し、
  M1 の Issue が全て閉じているか（または M1完了後 / M2 へ移されているか）を確認

完了時に、上記の FR / NFR 対応表と、
./gradlew build ktlintCheck detekt test の結果、
統合テストの CI 実行件数（JUnit XML の実数）を報告してください。

M1 完了判定の表が、このプロジェクトの実質的な成果物になります。「設計書に書いた24の機能要件のうち、何がどこまで動くのか」が根拠付きで一覧化されている状態を作ってください。推測で埋めると後で必ず食い違うので、判定はコードを確認したうえで行うこと。

保存したら、まず方針2点（NFR-002 の扱い、性能測定の方法）を提示してください。実装はその承認後で構いません。
