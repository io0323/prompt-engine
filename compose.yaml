services:
  postgres:
    image: postgres:16
    # ロール分離（Issue #85、ADR-0036）: コンテナのブートストラップロール（実質superuser）は
    # prompt_engine_migrator とする。アプリ実行時ロール prompt_engine（restricted、追記専用
    # テーブルへはINSERT/SELECTのみ）はFlyway migration V20が冪等に作成する
    # （application.ymlのspring.flyway.*参照）。既存のpostgres-dataボリュームを使い回すと
    # このロール分離が反映されない（POSTGRES_USER/PASSWORDはinitdb時のみ有効なため）。
    # 既存ボリュームがある場合は `docker compose down -v` 等でボリュームごと作り直すこと。
    environment:
      POSTGRES_DB: prompt_engine
      POSTGRES_USER: prompt_engine_migrator
      POSTGRES_PASSWORD: prompt_engine_migrator
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U prompt_engine_migrator -d prompt_engine"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

  redpanda:
    image: docker.redpanda.com/redpandadata/redpanda:latest
    command:
      - redpanda
      - start
      - --smp=1
      - --overprovisioned
      - --node-id=0
      - --kafka-addr=PLAINTEXT://0.0.0.0:9092
      - --advertise-kafka-addr=PLAINTEXT://localhost:9092
    ports:
      - "9092:9092"
    volumes:
      - redpanda-data:/var/lib/redpanda/data
    healthcheck:
      test: ["CMD-SHELL", "rpk cluster health | grep -q 'Healthy:.*true'"]
      interval: 10s
      timeout: 5s
      retries: 10

  opensearch:
    image: opensearchproject/opensearch:2
    environment:
      discovery.type: single-node
      DISABLE_SECURITY_PLUGIN: "true"
      OPENSEARCH_JAVA_OPTS: "-Xms512m -Xmx512m"
    ports:
      - "9200:9200"
    volumes:
      - opensearch-data:/usr/share/opensearch/data
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9200/_cluster/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10

volumes:
  postgres-data:
  redis-data:
  redpanda-data:
  opensearch-data:
